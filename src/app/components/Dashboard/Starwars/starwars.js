import React from 'react'
const Starwars = ({ planets }) => {
	return (
		<div>
			<h3>pick a thing any thing</h3>
			{/*planets.map((planet, index ) => (
				<Header key={index} >{planet.name}</Header>
			))}*/}
		</div>
	)
}

export default Starwars